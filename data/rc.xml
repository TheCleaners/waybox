<?xml version="1.0" encoding="UTF-8"?>
<openbox_config xmlns="http://openbox.org/3.4/rc" xmlns:xi="http://www.w3.org/2001/XInclude">
  <!-- You can reserve a portion of your screen where windows will not cover when
       they are maximized, or when they are initially placed. -->
  <margins>
    <top>0</top>
    <bottom>0</bottom>
    <left>0</left>
    <right>0</right>
  </margins>
  <!-- Theme for compositor-drawn chrome (the root menu, and titlebars once
       server-side decorations land). <name> resolves an Openbox theme under
       <datadir>/themes/<name>/openbox-3/themerc (e.g. Clearlooks). Omit (as
       below) to use the built-in default; uncomment and set a name to theme it. -->
  <!-- <theme>
    <name>Clearlooks</name>
  </theme> -->
  <!-- waybox-only extensions (Openbox ignores this block). Menu behaviour:
       submenuOpen="hover|click", hoverDelay (ms before a submenu opens on
       hover), wrap (keyboard navigation wraps at the ends), icons (show
       per-item icons from the icon theme, when available). source selects the
       built-in native menu ("builtin", the default) or delegates to an external
       launcher command (e.g. source="rofi -show drun"). -->
  <waybox>
    <menu source="builtin" submenuOpen="hover" hoverDelay="100" wrap="yes" icons="yes"/>
    <switcher order="mru" osd="yes" wrap="yes"/>
  </waybox>
  <!-- Where newly-mapped windows are placed: Smart (avoid overlap, then
       cascade), Center, or UnderMouse. -->
  <placement>
    <policy>Smart</policy>
  </placement>
  <keyboard>
    <!-- XKB configuration.  See `man xkeyboard-config` for more information -->
    <!-- You can use the XKB_* environment variables instead -->
    <!-- <xkb>
      <model>105</model>
      <layout>us</layout>
      <options>ctrl:swapcaps,esperanto:dvorak,lv3:ralt_switch_multikey</options>
      <rules>evdev</rules>
      <variant>dvorak</variant>
    </xkb> -->
    <!-- Keybindings for windows -->
    <keybind key="A-F4">
      <action name="Close"/>
    </keybind>
    <keybind key="C-Escape">
      <action name="Exit"/>
    </keybind>
    <keybind key="W-c">
      <action name="Reconfigure"/>
    </keybind>
    <keybind key="W-m">
      <action name="Iconify"/>
    </keybind>
    <keybind key="W-S-M">
      <action name="ToggleMaximize"/>
    </keybind>
    <keybind key="F11">
      <action name="Fullscreen"/>
    </keybind>
    <keybind key="W-t">
      <action name="ToggleDecorations"/>
    </keybind>
    <!-- Key chains: press the prefix, then the next key. e.g. W-w then f
         launches a browser, W-w then t a terminal. -->
    <!-- <keybind key="W-w">
      <keybind key="f"><action name="Execute"><command>x-www-browser</command></action></keybind>
      <keybind key="t"><action name="Execute"><command>$TERMINAL</command></action></keybind>
    </keybind> -->
    <keybind key="W-s">
      <action name="Shade"/>
    </keybind>
    <keybind key="W-S-S">
      <action name="Unshade"/>
    </keybind>
    <!-- Keybindings for window switching -->
    <keybind key="A-Tab">
      <action name="NextWindow"/>
      <action name="Unshade"/>
    </keybind>
    <keybind key="A-S-ISO_Left_Tab">
      <action name="PreviousWindow"/>
      <action name="Unshade"/>
    </keybind>
    <keybind key="C-A-Tab">
      <action name="NextWindow"/>
      <action name="Unshade"/>
    </keybind>
    <!-- Keybindings for running applications -->
    <keybind key="W-Return">
      <action name="Execute">
        <execute>$TERMINAL</execute>
      </action>
    </keybind>
    <keybind key="Print">
      <action name="Execute">
        <execute>grim "$(xdg-user-dir PICTURES)/$(date +'%Y-%m-%d-%H%M%S_grim_fs.png')"</execute>
      </action>
    </keybind>
    <keybind key="A-Sys_Req">
      <action name="Execute">
        <execute>grim -g "$(slurp -d)" "$(xdg-user-dir PICTURES)/$(date +'%Y-%m-%d-%H%M%S_grim_sel.png')"</execute>
      </action>
    </keybind>
    <keybind key="A-r">
      <action name="Execute">
        <!-- waybox-menu is a rofi script mode; it must be passed via -modi.
             If you'd prefer something like wmenu: `waybox-menu | wmenu | xargs env` -->
        <execute>rofi -modi waybox-menu:waybox-menu -show waybox-menu</execute>
      </action>
    </keybind>
    <keybind key="A-F2">
      <action name="Execute">
        <execute>rofi -show drun</execute>
      </action>
    </keybind>
    <keybind key="W-e">
      <action name="Execute">
        <execute>konqueror || falkon</execute>
      </action>
    </keybind>
    <keybind key="W-o">
      <action name="Execute">
        <execute>$TERMINAL -e mc</execute>
      </action>
    </keybind>
    <keybind key="W-w">
      <action name="Execute">
        <execute>x-www-browser</execute>
      </action>
    </keybind>
    <!-- Media keys -->
    <keybind key="XF86AudioMute">
      <action name="Execute">
        <execute>amixer -c 0 -D pulse -q sset Master toggle</execute>
      </action>
    </keybind>
    <keybind key="XF86AudioLowerVolume">
      <action name="Execute">
        <execute>amixer -c 0 -q set Master 3%-</execute>
      </action>
    </keybind>
    <keybind key="XF86AudioRaiseVolume">
      <action name="Execute">
        <execute>amixer -c 0 -q set Master 3%+</execute>
      </action>
    </keybind>
  </keyboard>
  <!-- Configuration for mice and other pointers. <mouse> instead of <pointer> only for backwards compatibility. -->
  <mouse>
    <!-- libinput configuration. -->
    <!-- <libinput>
      <accelProfile>adaptive</accelProfile>
      <accelSpeed>0.9</accelSpeed>
      <calibrationMatrix>0 0 0 0 0 0</calibrationMatrix>
      <clickMethod>buttonAreas</clickMethod>
      <disableWhileTrackpointing>enabled</disableWhileTrackpointing>
      <disableWhileTyping>enabled</disableWhileTyping>
      <leftHanded>disabled</leftHanded>
      <middleEmulation>enabled</middleEmulation>
      <naturalScroll>disabled</naturalScroll>
      <scrollButton>BTN_MIDDLE</scrollButton>
      <scrollButtonLock>disabled</scrollButtonLock>
      <scrollMethod>twofinger</scrollMethod>
      <tap>enabled</tap>
      <tapButtonMap>lrm</tapButtonMap>
      <tapDrag>enabled</tapDrag>
      <tapDragLock>enabled</tapDragLock>
    </libinput> -->
    <!-- Right-click the desktop to open the native root menu (defined in
         menu.xml). -->
    <context name="Root">
      <mousebind button="Right" action="Press">
        <action name="ShowMenu"><command>root-menu</command></action>
      </mousebind>
    </context>
  </mouse>
  <!-- Per-application rules. `class`/`name` match a window's app_id and
       `title` matches its title (glob patterns: * and ?). Overrides:
       position/size, maximized, fullscreen, iconic, focus, decor. -->
  <!-- <applications>
    <application class="mpv">
      <fullscreen>yes</fullscreen>
    </application>
    <application class="foot" title="*scratch*">
      <position><x>200</x><y>120</y></position>
      <size><width>900</width><height>600</height></size>
      <decor>no</decor>
    </application>
  </applications> -->
</openbox_config>
